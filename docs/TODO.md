# TODO (отложенные задачи)

## Завести почтовый ящик support@botisk.ru
**Приоритет:** средний · **Заведено:** 2026-06-03

На странице (`index.html`) и в JSON-LD указан контакт **support@botisk.ru**,
но самого ящика пока нет (создание долго). Адрес `videoai.ru` использовать
нельзя — это не наш домен.

**Что сделать:**
- Завести ящик/алиас `support@botisk.ru` (или форвард на рабочую почту/Telegram).
- Проверить доставку: письмо с формы поддержки и по `mailto:` реально доходит.
- Пока ящик не работает — основной канал поддержки остаётся Telegram-бот
  `@VideoAI_24isk_bot` (форма поддержки шлёт в `n8n.24isk.ru/webhook/support`).

**Где упоминается адрес:** `index.html` — секция контактов (~строка 1776),
блок поддержки (~строка 1872) и JSON-LD Organization (~строка 44).
